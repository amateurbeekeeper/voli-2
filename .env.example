# Supabase: which project the app talks to is decided by which env you use.
# See docs/ENVIRONMENTS.md for local vs staging vs production.

# --- LOCAL (npm run dev) ---
# Use your STAGING Supabase project so you never touch prod data.
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# --- STAGING (Vercel Preview) ---
# Set these in Vercel → Project → Settings → Environment Variables → Preview.
# Same keys as a separate "staging" Supabase project.

# --- PRODUCTION (Vercel Production) ---
# Set these in Vercel → Project → Settings → Environment Variables → Production.
# Your production Supabase project only.
