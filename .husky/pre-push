#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Full suite (lint + unit test + build) on every push to any branch.
# To include E2E as well: PREPUSH_FULL=1 git push
if [ -n "$PREPUSH_FULL" ]; then
  npm run suite:e2e
else
  npm run suite
fi
